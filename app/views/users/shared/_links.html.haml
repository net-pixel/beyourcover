- if controller_name != 'sessions'
  = link_to "ログイン", new_session_path(resource_name), class: 'showMain__content__topContent__itemBox__detail__btn__purchaseBtn'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  .showMain__content__topContent__itemBox__detail__btn
    = link_to "新規登録", new_user_path, class: "showMain__content__topContent__itemBox__detail__btn__purchaseBtn"
  %br/
-# - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'　/パスワード確認機能削除
-#   = link_to "Forgot your password?", password_path(resource_name)　/パスワード確認機能削除
  %br/
- if devise_mapping.confirmable? && controller_name != 'confirmations'
  = link_to "確認メールを受け取っていない方はこちら？", new_confirmation_path(resource_name), class: "noeffect"
  %br/
- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  = link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name), class: "btn-default btn-sns"
  %br/
-# - if devise_mapping.omniauthable?
-#   - resource_class.omniauth_providers.each do |provider|
-#     = link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider)
-#     %br/
